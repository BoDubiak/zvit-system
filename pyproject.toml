[tool.ruff]
target-version = "py312"
line-length = 120
exclude = [
    "migrations",
    ".venv",
    "staticfiles",
    "media",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]
